<Window x:Class="TrayTrigger.Views.FolderBatchImportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:TrayTrigger.ViewModels"
        mc:Ignorable="d"
        Title="Batch Add Games from Folder"
        Height="560" Width="680"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="{StaticResource BrushDialogBg}"
        Foreground="{StaticResource BrushTextPrimary}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="Auto"/> <!-- Search & Bulk Selection -->
            <RowDefinition Height="*"/>    <!-- Games List -->
            <RowDefinition Height="Auto"/> <!-- Status & Action Buttons -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="0,0,0,16">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Add Games from Folder" FontSize="18" Foreground="{StaticResource BrushTextPrimary}" 
                           FontFamily="{StaticResource AppFont}" FontWeight="Normal"/>
                <TextBlock Text="{Binding SubtitleText}" 
                           FontSize="12" Foreground="{StaticResource BrushTextSecondary}" 
                           FontFamily="{StaticResource AppFont}" Margin="0,2,0,0"/>
            </StackPanel>
        </DockPanel>

        <!-- Filter & Selection Bar -->
        <Grid Grid.Row="1" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" 
                     Style="{StaticResource ModernTextBox}" Margin="0,0,12,0"
                     Tag="Search detected games..."/>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Select All" Command="{Binding SelectAllCommand}" 
                        Style="{StaticResource ModernGhostButton}" Margin="0,0,6,0"/>
                <Button Content="Deselect All" Command="{Binding DeselectAllCommand}" 
                        Style="{StaticResource ModernGhostButton}"/>
            </StackPanel>
        </Grid>

        <!-- Games List -->
        <Border Grid.Row="2" CornerRadius="8" Background="{StaticResource BrushSurfaceDark}" 
                BorderBrush="{StaticResource BrushBorderDark}" BorderThickness="1" Margin="0,0,0,16">
            <ListBox ItemsSource="{Binding FilteredGames}" 
                     Background="Transparent" BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#222228"/>
                        <Setter Property="Padding" Value="10,8"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border Background="{TemplateBinding Background}" 
                                            BorderBrush="{TemplateBinding BorderBrush}" 
                                            BorderThickness="{TemplateBinding BorderThickness}" 
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{StaticResource BrushSurfaceHover}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:BatchGameItemViewModel}">
                        <DockPanel LastChildFill="True">
                            <!-- Checkbox -->
                            <CheckBox IsChecked="{Binding IsSelected}" 
                                      VerticalAlignment="Center" Margin="0,0,12,0"
                                      DockPanel.Dock="Left">
                                <CheckBox.Style>
                                    <Style TargetType="CheckBox" BasedOn="{StaticResource ModernCheckBox}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsAlreadyImported}" Value="True">
                                                <Setter Property="IsEnabled" Value="False"/>
                                                <Setter Property="Opacity" Value="0.4"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </CheckBox.Style>
                            </CheckBox>

                            <!-- Game Icon -->
                            <Border Width="36" Height="36" CornerRadius="6" Background="#22222A" 
                                    Margin="0,0,12,0" VerticalAlignment="Center" DockPanel.Dock="Left">
                                <Image Source="{Binding IconImage}" Width="28" Height="28" 
                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                       RenderOptions.BitmapScalingMode="HighQuality"/>
                            </Border>

                            <!-- Badges (Right Dock) -->
                            <StackPanel DockPanel.Dock="Right" VerticalAlignment="Center" Margin="10,0,0,0" HorizontalAlignment="Right">
                                <TextBlock Text="{Binding DisplaySize}" FontSize="11" Foreground="{StaticResource BrushTextMuted}" 
                                           HorizontalAlignment="Right"/>
                                <Border Margin="0,3,0,0">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsAlreadyImported}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Border Background="#203022" CornerRadius="4" Padding="5,1.5">
                                        <TextBlock Text="IN LIBRARY" Foreground="#52C41A" FontSize="9" FontWeight="SemiBold"/>
                                    </Border>
                                </Border>
                            </StackPanel>

                            <!-- Name and Branch Path -->
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="{Binding Name}" FontSize="13.5" FontWeight="SemiBold"
                                           Foreground="{StaticResource BrushTextPrimary}" 
                                           FontFamily="{StaticResource AppFont}"/>
                                <TextBlock Text="{Binding DisplayPath}" FontSize="11" 
                                           Foreground="{StaticResource BrushTextSecondary}" 
                                           FontFamily="{StaticResource AppFont}"
                                           TextTrimming="CharacterEllipsis" Margin="0,2,0,0"/>
                            </StackPanel>
                        </DockPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- Footer -->
        <DockPanel Grid.Row="3">
            <TextBlock Text="{Binding SelectedCountDisplay}" FontSize="12" 
                       Foreground="{StaticResource BrushTextSecondary}" 
                       VerticalAlignment="Center"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Command="{Binding CancelCommand}" 
                        Style="{StaticResource ModernGhostButton}" Margin="0,0,8,0"/>
                <Button Content="{Binding ImportButtonLabel}" Command="{Binding ImportCommand}" 
                        IsEnabled="{Binding CanImport}"
                        Style="{StaticResource ModernAccentButton}"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
